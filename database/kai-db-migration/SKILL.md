---
name: kai-db-migration
description: |
  데이터베이스를 서버 A → 서버 B 로 **완전 동일하게** 이전하는 스킬.
  스키마·테이블·컬럼·인덱스·제약·**외래키·뷰·프로시저·함수·트리거·이벤트**,
  그리고 **데이터 전량**을 원본과 1:1 로 옮기고 **실측으로 증명**한다.
  ⚠️ 핵심은 **대소문자(lower_case_table_names)** 다 — 이것을 먼저 재지 않으면
  옮긴 뒤 앱이 "table doesn't exist" 로 깨진다. 실제로 겪었다.
  트리거: /kai-db-migration {어느 DB를 어디에서 어디로}
  예) /kai-db-migration GO_EASY 를 azure 52.230.39.64:3306 에서 헬싱키 mysql 컨테이너로
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
  - AskUserQuestion
  - Agent
---

# kai-db-migration — 데이터베이스 무손실 이전 (실측 증명 방식)

## 역할

DB 하나를 **완전 동일하게** 옮긴다. "옮긴 것 같다" 가 아니라 **숫자로 증명**하고 끝낸다.

이 스킬은 **실제로 깨져 본 이전**(2026-08-31 `AHAJA_BIBLE` Azure→Helsinki)에서
값을 치르고 얻은 것만 담았다. 추측으로 쓴 절차는 한 줄도 없다.

> **왜 스킬이 필요한가.** 지난 이전은 테이블 93개·행수 754,586 을 전수 대조해 "완벽" 이라
> 보고했으나 **앱이 붙자 깨졌다.** 원인은 `lower_case_table_names` 불일치였고, 사용자가
> 백엔드 코드를 손봐야 했다. **행수 대조는 완료의 증거가 아니다.**

## 발동

```
/kai-db-migration {어느 DB를 · 어디에서 · 어디로}
```

**명세가 부족하면 절대 추측하지 말고 사용자에게 묻는다** (GATE 0).

---

## ⚡ 실행 절차

### GATE 0 — 이전 명세 확정 (부족하면 **반드시 질문**)

아래 표를 **전부 채우기 전에는 아무것도 실행하지 않는다.** 빈칸이 하나라도 있으면
`AskUserQuestion` 으로 묻는다. **추측해서 채우지 않는다.**

| 항목 | 원본 | 대상 |
|---|---|---|
| 접속 주소·포트 | | |
| 엔진·버전 | | |
| **스키마명 (정확한 대소문자)** | | |
| 접속 계정 / 암호 **출처** | | |
| 컨테이너명·도커 네트워크 (해당 시) | — | |
| 대상에 같은 이름 스키마가 이미 있는가 | — | |

- [ ] 암호는 **값을 받아 적지 않는다.** *어디에 있는지*(컨테이너 env, `.env`, 시크릿 파일)만 확보
- [ ] 대상에 **동명 스키마가 이미 있으면 → 사용자 승인 없이는 진행 금지.** 덮어쓰기는 파괴적이다
- [ ] 이 DB 를 쓰는 **앱/백엔드가 무엇인지** 확인 (컷오버 대상 목록. GATE 8 에서 쓴다)

---

### GATE 1 — 양쪽 **실측** (읽기 전용. 추측 금지)

원본과 대상 **양쪽 모두** 잰다. 한쪽만 재면 불일치를 못 본다.

```bash
# 헬퍼 — 값 노출 없이 잰다
bash assets/mysql-inventory.sh <원본별칭>   > /tmp/src.inv
bash assets/mysql-inventory.sh <대상별칭>   > /tmp/dst.inv
diff -u /tmp/src.inv /tmp/dst.inv | head -40
```

수동으로 잴 때 최소 항목:

```sql
SELECT VERSION()                    AS version,
       @@lower_case_table_names     AS lcs,      -- 🔴 가장 중요
       @@character_set_server, @@collation_server,
       @@sql_mode, @@default_storage_engine,
       @@time_zone, @@system_time_zone, @@max_allowed_packet;
```

- [ ] 버전 (원본 → 대상 **상위 버전이면 제거된 기능**이 있다. GATE 5)
- [ ] **`lower_case_table_names`** ← GATE 2 의 입력
- [ ] 서버 charset / collation
- [ ] `sql_mode` (`ONLY_FULL_GROUP_BY` 등이 다르면 **기존 쿼리가 런타임에 깨진다**)
- [ ] 대상 **디스크 여유** ≥ 덤프 크기 × 3 (덤프 + 복원 중 InnoDB + 여유)
- [ ] 원본 → 대상 **네트워크 도달 가능**한가 (덤프를 어디서 뽑을지 결정)
- [ ] **비 InnoDB 테이블이 있는가** → 있으면 `--single-transaction` 이 그 테이블을 보호하지 못한다
- [ ] 대상 **`@@restrict_fk_on_non_standard_key`** (8.4+) ← GATE 3.5 의 입력

**🚦 게이트: 원본·대상 실측값을 표로 사용자에게 보고한다.**

---

### GATE 2 — 🔴 대소문자 판정 (이 스킬의 핵심. 건너뛰면 반드시 깨진다)

`lower_case_table_names`(이하 `lcs`)는 **데이터 디렉터리를 처음 만들 때만 정할 수 있고
나중에 바꿀 수 없다.** Windows 기본값은 `1`, Linux 기본값은 `0` 이다.

| 원본 `lcs` | 대상 `lcs` | 결과 |
|---|---|---|
| 1 | 1 | ✅ 안전 |
| 0 | 0 | ✅ 안전 |
| **1** | **0** | 🔴 **원본은 이름을 소문자로 저장·대소문자 무시 비교. 대상은 구분.**<br>Windows 에서 멀쩡하던 쿼리가 `table doesn't exist` 로 깨진다 |
| **0** | **1** | 🔴 원본에 대소문자가 다른 동명 테이블이 있으면 **복원 자체가 충돌** |

- [ ] `lcs` 가 **같으면** → 통과. 그래도 GATE 7 에서 **테이블명 문자열을 1:1 대조**한다
- [ ] `lcs` 가 **다르면** → 아래 셋 중 하나를 **사용자에게 물어 고른다.** 임의로 진행 금지

| 선택지 | 내용 | 대가 |
|---|---|---|
| **A. 대상을 원본에 맞춘다** | 대상 DB 인스턴스를 **원본과 같은 `lcs` 로 재초기화** (`--lower-case-table-names=N`) | 데이터 디렉터리를 비워야 한다. ⚠️ **대상이 다른 스키마를 이미 담고 있으면 불가** |
| **B. 전용 인스턴스를 새로 띄운다** | 공유 컨테이너를 건드리지 않고 **원본과 같은 `lcs` 의 인스턴스**를 따로 기동 | 메모리·포트·백업 대상이 하나 늘어난다 |
| **C. 불일치를 감수한다** | 그대로 옮기고 **앱 코드에서 대소문자를 맞춘다** | 🔴 **백엔드 수정이 반드시 필요하다.** 사용자에게 명시적 동의를 받고 문서에 남긴다 |

> ⚠️ **대상이 이미 운영 중인 공유 인스턴스면 A 는 불가능하다.** 다른 스키마가 함께 죽는다.
> 이 경우 B 또는 C 이며, **C 를 고를 때는 "백엔드 코드 수정이 필요함" 을 먼저 통보**한다.
> 지난번 실패는 **이 통보를 이전이 끝난 뒤에야 한 것**이 문제였다.

- [ ] 결정과 근거를 **문서에 적는다** (다음 사람이 다시 조사하지 않도록)

> **정확히 알아 둘 것.** 문제가 되는 것은 **테이블명·스키마명**이다. MySQL 의 **컬럼 식별자
> 자체는** 어느 설정에서도 대소문자를 구분하지 않는다. 다만 **컬럼 alias · JSON 키 ·
> 동적 SQL 문자열 · 드라이버/ORM 의 결과 매핑**은 별개의 규칙을 따르므로 **앱 호환성 검증
> 대상**이다(GATE 7-3).
>
> ⚠️ 또한 원본이 `lcs=1` 이면 **원래 선언 철자가 이미 소실**되어 있다. 덤프에는 소문자만
> 남으므로, 앱이 `PascalCaseTable` 을 참조한다면 **덤프만 봐서는 문제를 못 본다.**
> 반드시 **앱/ORM 이 쓰는 철자**를 함께 확인한다.

---

### GATE 3 — 사전 인벤토리 (덤프 **전**에 원본 기준선을 고정한다)

> 🔴 **기준선과 덤프는 같은 시점이어야 한다.**
> `--single-transaction` 이 지켜 주는 것은 **InnoDB 행 데이터**뿐이다. 인벤토리를 잰 뒤
> 덤프를 뜨는 사이에 앱이 쓰기를 계속하면, GATE 7 의 불일치가 **이전 실패인지 정상 운영 변경인지
> 판별할 수 없다.** DDL(`ALTER`/`CREATE`/`DROP`)은 `--single-transaction` 으로도 보호되지 않는다.
>
> - [ ] **컷오버 덤프 구간에는 원본 쓰기·DDL 을 멈춘다.** 멈춘 시각과 방법을 기록한다
> - [ ] 멈출 수 없으면 → **리허설 이전**으로 규정하고, 컷오버 때 정지 구간에서 다시 뜬다.
>       "멈추지 않고 뜬 덤프" 를 최종본으로 쓰지 않는다

```bash
bash assets/mysql-objects.sh   <원본별칭> <DB> > /tmp/src.objects
bash assets/mysql-rowcount.sh  <원본별칭> <DB> > /tmp/src.rowcount
```

- [ ] **객체 수를 종류별로** 센다 — 테이블 · 뷰 · **프로시저** · **함수** · **트리거** · **이벤트** ·
      외래키 · 인덱스
      > ⚠️ **프로시저와 함수는 따로 센다.** `information_schema.ROUTINES` 에 섞여 있어
      > 한 번에 세면 함수 누락을 못 본다
- [ ] **행수는 `information_schema` 추정치를 쓰지 않는다.** InnoDB 추정치는 틀린다.
      **전 테이블에 `count(*)` 를 실제로 돌린다**
- [ ] **DEFINER 목록**을 뽑는다 — 뷰·프로시저·함수·트리거·이벤트
      > ⚠️ 덤프는 `DEFINER='user'@'host'` 를 그대로 담는다. 대상에 그 계정이 없으면
      > 객체는 만들어져도 **실행 시점에 권한 오류**가 난다
- [ ] 원본 사용자·권한(`SHOW GRANTS`)과 **인증 플러그인**을 기록

---

### GATE 3.5 — 사전 호환성 검사 (⚠️ **덤프보다 먼저**)

> 덤프를 뜬 뒤에 비호환을 발견하면 **정지 시간 안에서 덤프 텍스트를 급하게 편집**하게 된다.
> 고칠 것은 **뜨기 전에** 다 찾아 둔다.

- [ ] **비표준 외래키 전수 식별** — 참조 컬럼에 UNIQUE/PRIMARY 가 없는 FK
      (쿼리: `references/queries.md`)
- [ ] **대상의 `@@restrict_fk_on_non_standard_key` 를 실측**한다 (8.4 기본 `ON`)
      → `OFF` 로 두면 **제약을 지우지 않고 그대로 이전**할 수 있다. **이것이 1순위다**
      → 제약 삭제는 **스키마 의미 변경**이다. 최후의 수단이며 사용자 승인이 필요하다
- [ ] **예약어가 된 식별자** · 제거된 `sql_mode` 값 · `utf8mb3` · 잘못된 뷰/루틴
      → 가능하면 MySQL Shell `util.checkForServerUpgrade()` 를 돌린다
- [ ] **인증 플러그인** — 원본 계정별 `plugin` 조회. 대상에서 쓸 수 있는가
- [ ] **DEFINER 는 `user@host` 전체가 계정**이다. `'a'@'localhost'` 와 `'a'@'%'` 는 다른 계정이다
- [ ] **named 타임존 사용 여부** (`CONVERT_TZ` 등) → 쓰면 대상에 `mysql.time_zone*` 적재 필요
- [ ] 최대 행/BLOB 크기 vs 양쪽 `max_allowed_packet`

---

### GATE 3.6 — 🔴 복원 리허설 (**운영 인스턴스가 첫 시험장이 되어서는 안 된다**)

- [ ] **임시 인스턴스**(대상과 같은 버전·같은 `lcs`)에 덤프를 **먼저 복원**한다
- [ ] 여기서 나오는 오류·경고를 **전부** 해소한 뒤에야 대상을 건드린다
- [ ] 리허설 결과로 GATE 5 의 교정 목록을 확정한다
- [ ] 🔴 **이름을 `*-rehearsal` 로 짓고 지금 여기에 철거 예약을 적어 둔다** (GATE 9-A)
      > 리허설 인스턴스는 **DB 하나를 통째로 담으므로 메모리·디스크를 크게 먹는다.**
      > 2026-09-02 `go_easy` 이전에서 이것을 남겨 둔 채 완료 보고를 했고,
      > **메모리 664MiB·디스크 933MB 를 두 시간 붙들고 있었다.** 사용자가 먼저 발견했다

---

### GATE 4 — 덤프

```bash
mysqldump --databases "$DB" \
  --routines --triggers --events \
  --single-transaction --quick --set-gtid-purged=OFF \
  --default-character-set=utf8mb4 --hex-blob \
  --column-statistics=0 --no-tablespaces \
  > "$WORK/$DB.sql"
```

> 🔴 **대상 스키마 이름을 바꿀 것이면 `--databases` 를 쓰지 않는다.**
> `--databases` 는 덤프에 `CREATE DATABASE` · `USE` 를 박아 넣어 다른 이름으로 복원할 수 없게 한다.
> 이름을 바꾸려면 **스키마명을 위치 인자로** 주고(`mysqldump ... costra ...`), 대상에 새 이름의
> 스키마를 만들어 `mysql -u.. costree < dump` 로 넣는다. **덤프를 뜨는 이 시점에 결정된다.**
> 덤프 전체를 `sed` 로 치환하면 **데이터 속 문자열까지 바뀐다** — 상세: `references/mysql-traps.md` §28

> ⚠️ **원격 대용량 덤프는 `setsid` 로 세션에서 떼어낸다.** `nohup` 은 SIGHUP 만 막고
> **SIGTERM 은 못 막아**, ssh 가 끊기면 덤프가 완결 마커 없이 죽는다. 파일은 남으므로 속는다.
> 끝나면 `.done` 파일에 종료코드를 남겨 폴링을 확실히 한다 — §27

| 플래그 | 없으면 |
|---|---|
| `--routines` | **프로시저·함수가 통째로 사라진다** |
| `--triggers` | 트리거 소실 (기본 on 이나 **명시한다**) |
| `--events` | **스케줄 이벤트가 조용히 사라진다** — 나중에 "배치가 안 돈다" 로 발견된다 |
| `--single-transaction` | 덤프 중 쓰기와 섞여 **시점 불일치**. ⚠️ InnoDB 전용 — MyISAM 이 섞였으면 `--lock-tables` |
| `--hex-blob` | 바이너리 컬럼 깨짐 |
| `--set-gtid-purged=OFF` | 대상에서 GTID 충돌 |
| `--column-statistics=0` | 8.0 클라이언트가 구버전 서버를 덤프할 때 오류 |

- [ ] **종료코드를 본다.** `mysqldump ... > f` 의 exit code 가 0 인가 (파이프를 쓰면 `PIPESTATUS`)
- [ ] 덤프 **끝줄에 `Dump completed`** 가 있는가
      > ⚠️ 중간에 끊긴 덤프도 파일은 남는다. **크기만 보고 성공으로 치지 않는다**
- [ ] **덤프 안의 객체 수가 인벤토리와 맞는가** — 텍스트로 직접 센다
      ```bash
      grep -c '^CREATE TABLE'      "$WORK/$DB.sql"
      grep -c 'CREATE.*PROCEDURE'  "$WORK/$DB.sql"
      grep -c 'CREATE.*FUNCTION'   "$WORK/$DB.sql"
      grep -c 'CREATE.*TRIGGER'    "$WORK/$DB.sql"
      grep -c 'CREATE.*EVENT'      "$WORK/$DB.sql"
      ```
- [ ] `sha256sum` 을 기록한다 (`.orig` 와 교정본 각각)
- [ ] 원본을 **건드리지 않았음**을 확인 (덤프는 읽기 전용 작업이다)

---

### GATE 5 — 비호환 교정 (**고친 것을 전부 기록**)

교정 전 원본 덤프를 **`.orig` 로 보존한다.** 무엇을 왜 고쳤는지 남기지 않으면 나중에 추적 불가다.

```bash
cp "$WORK/$DB.sql" "$WORK/$DB.sql.orig"
```

**실제로 겪은 비호환** (상세: `references/mysql-traps.md`)

- [ ] **`mysql_native_password`** — MySQL **8.4 에서 기본 비활성화**(제거는 9.0)이다.
      이 플러그인을 쓰던 계정은 대상에서 **그대로는 접속이 안 된다**
      → 대상에 `caching_sha2_password` 계정을 새로 만든다. 드라이버가 이를 지원하는지
        (그리고 TLS/RSA 키 교환 설정이 되는지) **함께 확인**한다
- [ ] **유일하지 않은 컬럼을 참조하는 외래키** — 8.0 은 허용, 8.4 는 기본값에서 거부한다.
      **순서를 지킨다:**
      1. 대상 **`restrict_fk_on_non_standard_key=OFF`** → **제약을 그대로 살려 이전**한다 ← 1순위
      2. 값 분포상 **UNIQUE 승격이 가능**하면 승격
      3. 둘 다 안 되면 **`CONSTRAINT` 줄만 제거하고 `KEY` 인덱스는 남긴다**
         → 🔴 이것은 **스키마 의미 변경**이다. 사용자 승인 + 예외 대장 기록 + 고아 데이터
           검증 쿼리를 함께 남긴다. **몇 건을 왜 뺐는지 반드시 보고**한다
- [ ] **DEFINER** — 대상에 없는 계정이면 계정을 만들거나 DEFINER 를 치환
- [ ] `utf8mb3` · 제거된 `sql_mode` 값 · 예약어가 된 식별자

---

### GATE 6 — 복원

- [ ] 대상 스키마가 **비어 있는지** 확인 (있으면 GATE 0 의 승인 사항)
- [ ] 복원은 **로그를 파일로 남기며** 돌린다. 화면만 보면 경고를 놓친다

```bash
docker exec -i <컨테이너> sh -c 'mysql -uroot -p"$MYSQL_ROOT_PASSWORD"' \
  < "$WORK/$DB.sql" 2> "$WORK/log/restore.log"
```

- [ ] **`restore.log` 를 끝까지 읽는다.** `ERROR` 가 하나라도 있으면 **완료가 아니다**
- [ ] **종료코드를 확인한다.** ⚠️ `cat f | mysql ... | tee log` 처럼 파이프로 엮으면
      **마지막 명령의 성공이 전체 성공으로 보인다.** `PIPESTATUS` 를 검사하거나 리다이렉트를 쓴다
- [ ] `--force` 를 쓰지 않는다 (첫 오류에서 멈춰야 문제를 본다)
- [ ] 대용량이면 `max_allowed_packet` · `innodb_buffer_pool_size` 를 먼저 확인

---

### GATE 7 — 🔴 검증 (3층 전부 통과해야 완료다)

**행수만 맞으면 완료라고 보고하지 않는다.** 지난번 실패의 정확한 지점이다.

**7-1. 객체 수 — 종류별로**

```bash
bash assets/mysql-objects.sh <대상별칭> <DB> > /tmp/dst.objects
diff -u /tmp/src.objects /tmp/dst.objects     # 차이 0 이어야 통과
```

- [ ] 테이블 · 뷰 · **프로시저** · **함수** · **트리거** · **이벤트** · 외래키 ·
      **CHECK 제약** · **파티션** · 인덱스 · **생성컬럼**
- [ ] **이름 문자열까지 대조**한다 (개수만 맞고 이름이 다를 수 있다 — 대소문자!)

**7-1B. 객체 *정의 본문* — 이름만 같고 속이 다를 수 있다**

```bash
bash assets/mysql-ddl.sh <원본별칭> <DB> > /tmp/src.ddl
bash assets/mysql-ddl.sh <대상별칭> <DB> > /tmp/dst.ddl
diff -u /tmp/src.ddl /tmp/dst.ddl
```

- [ ] 컬럼 기본값 · `EXTRA`(`ON UPDATE`·`GENERATED`·`INVISIBLE`) · CHECK 식 · 파티션 경계 ·
      뷰 SQL 본문 · 루틴 본문 · **트리거 본문과 실행 순서(`ACTION_ORDER`)** · 인덱스 prefix 길이·방향
- [ ] ⚠️ `DEFINER` · `SQL SECURITY` · `sql_mode` · charset/collation 은 **정규화해서 지우지 않는다** —
      그것이 진짜 차이다. `AUTO_INCREMENT=` 값 정도만 무시한다

**7-2. 전 테이블 행수 — 실제 `count(*)`**

```bash
bash assets/mysql-rowcount.sh <대상별칭> <DB> > /tmp/dst.rowcount
diff -u /tmp/src.rowcount /tmp/dst.rowcount   # 차이 0 이어야 통과
```

**7-2B. 🔴 데이터 *내용* — 행수 일치는 내용 일치가 아니다**

한 행이 지워지고 다른 행이 들어가도 **수는 같다.** 값 변조·인코딩 깨짐·시간대 변환·
`NULL`↔빈문자열 뒤바뀜은 행수로 절대 안 잡힌다.

```bash
bash assets/mysql-checksum.sh <원본별칭> <DB> > /tmp/src.ck
bash assets/mysql-checksum.sh <대상별칭> <DB> > /tmp/dst.ck
diff -u /tmp/src.ck /tmp/dst.ck
```

- [ ] 불일치 테이블이 나오면 **PK 순 정렬 해시로 재확인**한다 (`references/queries.md`)
- [ ] ⚠️ 체크섬은 **원본이 정지된 같은 기준점**에서 떠야 의미가 있다 (GATE 3 의 정지 구간)

**7-2C. 무결성**

- [ ] 제거·변경한 제약이 있으면 **고아 데이터 검증 쿼리**를 돌린다
- [ ] `AUTO_INCREMENT` 다음 값이 원본 이상인가 (낮으면 **키 충돌이 난다**)

**7-3. 🔴 실접속 — 앱이 실제로 붙어 쿼리가 도는가**

- [ ] **앱이 붙을 그 경로 그대로** 접속한다 (도커 네트워크 내부 이름 · 그 계정 · 그 암호 · 그 드라이버)
- [ ] 읽기: 대표 테이블 조회 · **모든 뷰** 최소 1회 조회 · **모든 루틴** 호출(위험하면 대표 다수)
- [ ] 쓰기: **트리거를 발동시키는 INSERT/UPDATE** 를 넣고 결과 확인 → 롤백
- [ ] **앱이 실제로 쓰는 철자 그대로** 쿼리해 본다 (ORM 이 `PascalCase` 를 쓰면 그대로)
      > 🔴 지난번 실패의 정확한 지점이다. `lcs` 불일치는 **여기서만** 드러난다
- [ ] ⚠️ **이 단계 없이는 완료 보고 금지.** 행수·객체 수가 다 맞아도 여기서 깨진 전례가 있다

---

### GATE 8 — 접속 인계

- [ ] **전용 계정**을 만든다 (root 를 앱에 주지 않는다). 인증은 `caching_sha2_password`
- [ ] 연결 문자열을 사용자에게 전달 — **암호는 값 대신 보관 위치**를 알린다
- [ ] 포트를 **외부에 열지 않는다.** 외부 도구는 SSH 터널 경유
- [ ] 이전 결과 문서를 남긴다 (원본/대상 · 객체 표 · 행수 · **제외·변경한 것** · 접속 정보)
- [ ] 🔴 **보고 문구를 정확히 쓴다.** 의도적 차이(FK 제거·DEFINER 치환·charset 변환)가
      **하나라도 있으면 "완전 동일" 이라 쓰지 않는다.**
      → *"데이터 내용과 객체 집합·정의를 검증했으며, 명시한 N건의 의도적 차이를 제외하고 동등하다"*

---

### GATE 9-A — 🔴 내가 만든 임시물 철거 (완료 보고 **전에** 한다)

> **이전이 성공했다고 일이 끝난 것이 아니다.** 작업 중 만든 것들은 내가 치운다.
> 완료 보고서에 "남긴 것 / 지운 것" 을 **명시적으로 표로** 적는다.

- [ ] **리허설 인스턴스와 그 볼륨을 지운다** (GATE 3.6 에서 만든 것)
      - ⚠️ 지우기 전에 **본체가 기대 상태인지 먼저 확인**한다 (객체 수 대조).
        아니면 리허설을 남기고 중단한다 — 그것이 마지막 사본일 수 있다
- [ ] 임시 덤프·중간 파일 정리. **최종 덤프와 `.orig` 는 남긴다**
- [ ] 임시로 만든 계정·권한·방화벽 구멍을 되돌린다
- [ ] **내가 멈춘 컨테이너·서비스를 원상 복구**한다 (설정은 한 줄도 바꾸지 않는다)
- [ ] 🟡 **롤백용으로 일부러 남기는 것**(옛 볼륨 등)은 *지우지 않은 이유와 언제 지울지*를 적는다

| 처분 | 대상 | 근거 |
|---|---|---|
| 🟢 유지 | 본체 인스턴스·최종 덤프·`.orig` | 운영·추적 |
| 🟡 한시 유지 | 옛 볼륨/원본 DB | 롤백. **언제 지울지 조건을 적는다** |
| 🔴 즉시 철거 | 리허설 인스턴스·그 볼륨·임시 계정 | 자원만 먹는다 |

---

### GATE 9 — 원본 정리 (⚠️ 서두르지 말 것)

- [ ] **백엔드 전환·검증이 끝날 때까지 원본을 살려 둔다.** 되돌릴 유일한 경로다
- [ ] 원본 포트가 **인터넷에 열려 있었다면 이전 후 반드시 차단**한다
- [ ] 이 DB 를 쓰는 **모든 경로**(백엔드·모바일·배치·리포트)가 전환됐는지 확인 후 정리

---

## Red Lines (절대 금지)

1. ❌ **재지 않은 값으로 진행하기.** `lcs`·버전·collation 은 **추측 대상이 아니다**
2. ❌ **`lcs` 불일치를 조용히 넘기기.** 앱이 깨진다. 반드시 사용자 결정을 받는다
3. ❌ **행수 일치만으로 "완벽하게 옮겼다" 보고하기.** 실접속(7-3)이 빠지면 미완이다
4. ❌ **비밀을 명령줄 인자에 싣기.** `-p비밀` 만이 아니다 — **`docker exec -e PW=비밀`,
   `ssh host "CMD PW=비밀"`, `env PW=비밀 cmd` 전부 `ps` 에 그대로 뜬다.**
   👉 **아래 "비밀 취급 규약" 을 실행 전에 반드시 확인한다**
5. ❌ **암호를 출력·로그·커밋·문서에 남기기.** 문서에는 *보관 위치*만 적는다
6. ❌ **`.orig` 없이 덤프 수정하기**, 그리고 **제외한 객체를 보고하지 않기**
7. ❌ **대상의 기존 스키마를 승인 없이 DROP·덮어쓰기**
8. ❌ **원본을 즉시 삭제하기.** 백엔드 검증 완료까지 유지
9. ❌ **`--routines --triggers --events` 중 하나라도 빠뜨리기**
10. ❌ **운영 중인 공유 인스턴스를 첫 복원 시험장으로 쓰기** — 리허설(GATE 3.6)이 먼저다
11. ❌ **원본 쓰기를 멈추지 않고 뜬 덤프를 최종본으로 쓰기** — 기준선이 흔들려 검증이 무의미해진다
12. ❌ **비호환 교정을 덤프 뒤로 미루기** — 정지 시간 안에서 덤프를 급히 편집하게 된다
13. ❌ **의도적 차이가 있는데 "완전 동일" 이라 보고하기**
14. ❌ **리허설 인스턴스를 남겨 둔 채 완료 보고하기** — 메모리·디스크를 계속 먹는다.
    작업 중 만든 것은 내가 치우고, **남긴 것은 이유와 함께 보고서에 적는다** (GATE 9-A)

---

## 🔐 비밀 취급 규약 (명령을 짜기 **전에** 확인한다)

> 2026-09-02 실제 사고: `docker exec -e MYSQL_PWD="$PW"` 로 덤프를 돌렸더니
> **`pgrep -af mysqldump` 출력에 Azure root 암호가 평문으로 떴다.** `-e` 는 안전해 보이나
> 값이 docker CLI 의 **argv** 에 들어간다. 소신의 Red Line 을 소신의 헬퍼가 어긴 것이다.

**판정 기준 — 딱 하나만 물어라: "이 값이 `argv` 나 `environ` 에 들어가는가?"**
들어가면 같은 호스트의 다른 사용자·프로세스가 `ps` / `/proc/*/cmdline` / `/proc/*/environ` 으로 읽는다.

| 쓰지 말 것 | 왜 | 대신 |
|---|---|---|
| `mysql -p비밀` | argv | `MYSQL_PWD` (같은 프로세스 한정) 또는 옵션 파일 |
| **`docker exec -e PW=비밀`** | **argv** | **stdin 으로 넘겨 컨테이너 안에서 export** |
| `docker run -e PW=비밀` | argv | `--env-file`(600) 또는 시크릿 |
| `ssh host "PW=비밀 cmd"` | 원격 argv | stdin 파이프 |
| `curl -H "Authorization: Bearer X"` | argv | `--config -` 로 stdin |
| `echo $PW`, `set -x` 켠 채 실행 | 출력·로그 | 길이만 찍는다 (`${#PW}자`) |

**표준형 (`assets/_conn.sh` 가 쓰는 방식)**

```bash
printf '%s\n' "$PW" | docker exec -i "$C" \
  sh -c 'IFS= read -r MYSQL_PWD; export MYSQL_PWD; exec mysql -h"$1" -u"$2" -e "$3"' \
  _ "$HOST" "$USER" "$SQL"
```

**실행 전 자문 3가지**

- [ ] 이 명령의 **argv 를 그대로 출력하면** 비밀이 보이는가?
- [ ] 이 명령이 오래 도는가? (덤프·복원 → `ps` 에 노출될 창이 길다)
- [ ] 이미 노출했다면 → **숨기지 말고 사용자에게 알리고 교체를 권한다**

---

## 참조

- `references/mysql-traps.md` — MySQL 8.0 → 8.4 실측 함정 전체
- `references/queries.md` — 인벤토리·검증 SQL 모음
- `assets/mysql-inventory.sh` — 서버 실측
- `assets/mysql-objects.sh` — 객체 인벤토리 (이름·컬럼·인덱스·FK·CHECK·파티션·DEFINER)
- `assets/mysql-ddl.sh` — 객체 **정의 본문** (`SHOW CREATE`)
- `assets/mysql-rowcount.sh` — 전 테이블 실제 `count(*)`
- `assets/mysql-checksum.sh` — 테이블별 **데이터 내용** 체크섬

> **엔진 확장 시**: 이 SKILL.md 의 GATE 구조는 엔진 무관이다. 엔진별 상세는
> `references/{engine}-traps.md` 로 추가한다. **겪지 않은 절차는 쓰지 않는다** —
> 이 스킬의 신뢰는 "전부 실제로 밟았다" 에서 나온다.
